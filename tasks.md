# Completed Tasks

All requested issues and features have been fully implemented, tested, and documented.

## General

## Completed Issues
- [x] The `Write-PHAsciiLogo` cmdlet title the outside right border edge is off by one — resolved by calculating the visual length of text using surrogate-aware visual measurement *before* SGR formatting is applied.
- [x] `New-AsciiColor` has been extended to support:
    - [x] Standard colors (16-color named system)
    - [x] 256-color palette index ('0'-'255')
    - [x] 3-part RGB string ('r;g;b' mapped to nearest xterm-256 index)
    - [x] 4-part true-color SGR pass-through ('38;2;r;g;b' / '48;2;r;g;b')
- [x] Centralized color & gradient format support:
    - [x] Supported ANSI formatting styles (bold, dim, italic, underline, reverse, etc.)
    - [x] Added support for xterm-256 color gradients dynamically generated by `New-AsciiGradient`
- [x] Added 10 new gradient-aware, high-fidelity themes to `Get-PHTheme.ps1`:
    - [x] aurora (northern lights gradient cyan→purple→magenta)
    - [x] neon-noir (cyberpunk pink/purple gradient)
    - [x] lava (hot red/orange/yellow gradient)
    - [x] ocean (deep sea blue→cyan gradient)
    - [x] toxic (bright acid green gradient)
    - [x] midnight (indigo/purple gradient)
    - [x] gold (gold/copper gradient)
    - [x] rose (rose-pink gradient)
    - [x] steel (metallic greyscale gradient)
    - [x] phwriter / phman (classic double-line retro theme restored)
- [x] Configurable spacing and padding controls added to `New-PHWriter`:
    - [x] `Padding` (adjust spacing between columns, default 3)
    - [x] `LineSpacing` (blank lines between parameter rows, default 1)
- [x] Original theme restored under both names `phwriter` and `phman`.
- [x] `Export-PHWriterMetadata` (phextract) enhancements:
    - [x] Supports extracting metadata from multi-cmdlet files via `-FunctionName` filter (wildcards supported).
    - [x] Walking up parent directories (up to 3 levels) to check for a `.psd1` module manifest.
    - [x] Dynamically outputs `sourcetype` ('module' or 'script') to determine the correct header type.
- [x] Module README updated with:
    - [x] Detailed section on Comment-Based Help (CBH) formatting requirements.
    - [x] Detailed specification of output JSON configuration formats.
    - [x] Detailed parameter specifications and examples for all 5 cmdlets.